[project]
name = "KIA-Opensource-Release-Presentation"
version = "0.1.0"
description = "Using NotebookLM to generate the presentation for the openchain event"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "notebooklm-py[browser]>=0.3.4",
]
